import Modal from "../UI/Modal";
const Success = () => {
    return (
        <Modal>
            <p>Order Placed Successfully!</p>
            <button>Close</button>
        </Modal>
    );
};

export default Success;